import "./navbar.scss";

export default function Navbar() {
  return (
    <div class="navbar">
      <div class="container">
        <div class="left">
          {/* font awesome icon for logo */}
          {/* Browse, community, and searchbar */}
        </div>
        <div class="right">
          {/* hamburger menu */}
        </div>
      </div>
    </div>
  );
}
